rm -rf dist/*
pip install build
python3 -m build
if [ "$1" == "main" ]; then
twine upload dist/infercode-*
pip3 uninstall infercode -y
pip3 install infercode
else
pip3 uninstall infercode -y
pip3 install dist/infercode-*-py3-none-any.whl
python3 -c "import infercode"
fi
